export default function Navbar() {
  return (
    <nav
      style={{
        padding: 20,
        background: "#f0f0f0",
        color: "#000",
        borderBottom: "1px solid #ddd"
      }}
    >
      <div
        style={{
          fontWeight: "700",
          fontFamily: "Poppins, sans-serif",
          textAlign: "center",
          fontSize: 22
        }}
      >
        TROLO
      </div>
    </nav>
  );
}
